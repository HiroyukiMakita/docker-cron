COMPOSE_PROJECT_NAME=docker-cron

# タイムゾーン
TIMEZONE=Asia/Tokyo

# コンテナの自動再起動ポリシー
# - no：（または省略）自動再起動しない。コンテナが停止しても再起動しません。
# - always：常に再起動。異常終了でも正常終了でも、常に再起動します。ホスト再起動後も自動で起動します。
# - on-failure：異常終了したときのみ再起動。正常終了（exit code 0）の場合は再起動しません。
# - unless-stopped：常に再起動するが、手動で停止した場合は再起動しない。ホスト再起動後も自動起動されます（手動停止でなければ）。
RESTART_POLICY=always

# コンテナのネットワークモード
# - bridge：デフォルトのブリッジネットワークを使用します。
# - host：ホストのネットワークスタックを使用します。コンテナはホストと同じIPアドレスを共有します。
# - none：コンテナはネットワークに接続されません。
# - container:<name|id>：指定したコンテナのネットワークスタックを使用します。
# - service:<service_name>：指定したサービスのネットワークスタックを使用します。
# - <custom_network_name>：カスタムネットワークを使用します。
# ※ 注意: hostモードを使用する場合、セキュリティ上の理由から慎重に設定してください。
NETWORK_MODE=bridge